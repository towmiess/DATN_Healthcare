# -*- coding: utf-8 -*-
# ================================================================
# Healthcare RAG Chatbot — Toàn bộ thư viện cần thiết
# Cài đặt: pip install -r requirements.txt
# ================================================================

# --- Web Crawling (Bước 1: Thu thập tài liệu) ---
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0
fake-useragent==1.5.1

# --- PDF Processing (Bước 2: Chuẩn hóa tài liệu) ---
reportlab==4.2.0          # Tạo PDF từ text
PyMuPDF==1.24.1           # Đọc/ghi/xử lý PDF
fpdf2==2.7.9              # Tạo PDF hỗ trợ Unicode/tiếng Việt

# --- Text Chunking (Bước 3: Chia nhỏ văn bản) ---
langchain==0.2.6
langchain-community==0.2.6
langchain-text-splitters==0.2.2
tiktoken==0.7.0

# --- Embeddings & Vector DB (Bước 4: Kho tri thức số) ---
sentence-transformers==3.0.1   # Embedding đa ngôn ngữ (có tiếng Việt)
chromadb==0.5.3                # Vector DB local, không cần cloud
posthog<4.0.0                  # Tương thích telemetry client của chromadb 0.5.x

# --- LLM (Bước 5: Sinh câu trả lời) ---
# Gemini dùng REST API qua requests, không cần SDK riêng.

# --- API Server ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Tiện ích ---
python-dotenv==1.0.1
tqdm==4.66.4
loguru==0.7.2
pydantic==2.7.4
chardet<6.0.0                  # Tránh warning RequestsDependencyWarning trên Windows
